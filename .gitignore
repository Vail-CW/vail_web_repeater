# Build artifacts
build/*.exe
build/vail

# Deployment files (contain secrets/credentials)
DEPLOY.md
deploy.bat
deploy.sh

# Root Dockerfile is a deploy-time artifact for `gcloud run deploy --source .`
# (copied from build/Dockerfile by the deploy process). The canonical
# Dockerfile lives at build/Dockerfile and IS tracked.
/Dockerfile

# Local data files
events.json

# Claude Code / AI assistance files
.claude/
CLAUDE.md

# IDE and editor files
.idea/
.vscode/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Internationalization (not yet implemented)
i18n

# Environment files
.env
.env.local
.env.*.local
